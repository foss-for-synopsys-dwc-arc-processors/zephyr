sample:
  description: Comprehensive APLIC to IMSIC MSI delivery chain test
  name: APLIC MSI Test

common:
  tags:
    - riscv
    - interrupt
  integration_platforms:
    - qemu_riscv32/qemu_virt_riscv32_aia
  harness: console
  harness_config:
    type: multi_line
    regex:
      - "APLIC MSI Test"

tests:
  sample.drivers.aplic_msi_test:
    tags:
      - riscv
      - interrupt
    filter: >
      CONFIG_RISCV_APLIC_MSI and
      CONFIG_RISCV_IMSIC
