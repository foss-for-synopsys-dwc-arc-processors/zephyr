#include "rhx1xx.dtsi"

/ {
	/*
	 * Single DRAM / NVM region as per ARC TCF:
	 *   nsim_isa_nvm_base=0x80000000
	 *   nsim_isa_nvm_size=0x4000000
	 */
	ddr0: memory@80000000 {
		device_type = "memory";
		reg = <0x80000000 0x04000000>; /* 64 MB */
	};
};
